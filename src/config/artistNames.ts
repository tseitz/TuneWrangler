/**
 * Artist names whose `&` is part of the name, not a join between two artists. Everything else
 * with ` & ` is split into ` x ` collaborators. Hand-edited; matched case-insensitively.
 */
export const NAMES_WITH_AMPERSAND: readonly string[] = [
  "Joey Valence & Brae",
];
